using Dwellers.Household.Application.Features.Authentication.Commands.Register;
using Dwellers.Household.Contracts.Requests;
using System.Text.Json;

namespace Dwellers.Household.Tests.Contracts.Requests.Authentication
{
    //public class RegisterRequestTest
    //{
    //    [Fact]
    //    public void RegisterRequest_Serialization_Deserialization()
    //    {
    //        // Arrange
    //        var originalRequest = new RegisterRequest("test@example.com", "testuser", "testpassword");

    //        // Act
    //        var serializedJson = JsonSerializer.Serialize(originalRequest);
    //        var deserializedRequest = JsonSerializer.Deserialize<RegisterRequest>(serializedJson);

    //        // Assert
    //        Assert.NotNull(deserializedRequest);
    //        Assert.Equal(originalRequest.Email, deserializedRequest.Email);
    //        Assert.Equal(originalRequest.Alias, deserializedRequest.Alias);
    //        Assert.Equal(originalRequest.Password, deserializedRequest.Password);
    //    }
    //}
}
