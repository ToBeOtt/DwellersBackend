namespace Dwellers.Household.Tests.Contracts.Responses.Authentication
{
    //public class GetUserDetailsResponseTest
    //{
    //    [Fact]
    //    public void GetUserDetailsResponseTest_Serialization_Deserialization()
    //    {
    //        var user = new DwellerUser(); 
    //        var house = new House(); 

    //        var originalResponse = new GetUserDetailsResponse(user, house);

    //        var serializedJson = JsonSerializer.Serialize(originalResponse);
    //        var deserializedResponse = JsonSerializer.Deserialize<GetUserDetailsResponse>(serializedJson);

    //        // Using Shouldy-nuget to be able to compare objects with nested properties etc
    //        deserializedResponse.ShouldNotBeNull();
    //        deserializedResponse.User.ShouldBe(originalResponse.User);
    //        deserializedResponse.House.ShouldBe(originalResponse.House);
    //    }
    //}
}
